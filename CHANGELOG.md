![CubePay](cubepay-logo.png)

# 📋 Changelog

تمام تغییرات مهم این پروژه، به ترتیب زمانی، اینجا ثبت می‌شه.

---

## [2.1.9] — اصلاح کلید امضای کال‌بک VIP + مرجع آنلاین تعرفه‌ها

### Fixed

- **کلیدِ امضای کال‌بکِ VIP اصلاح شد — `sig` حالا با توکنِ `vip_` خودتان ساخته می‌شود**، همان توکنی که سفارش را با آن ساخته‌اید (نه توکنِ عادی). پیاده‌سازی‌های تک‌توکنه — از جمله فایل‌های آماده‌ی ربات‌ها — که قبلاً موقع برگشتِ مشتری خطای «امضای این کال‌بک معتبر نیست» می‌گرفتند، حالا بدون هیچ تغییری کار می‌کنند. قاعده‌ی ساده: هر کال‌بک با همان توکنی امضا می‌شود که سفارشش را ساخته‌اید.

### Added

- **🌐 مرجع آنلاین تعرفه‌ها: [cubevps.ir/fees.php](https://cubevps.ir/fees.php).** کارمزدها و شرایط هر دو نوع فروشنده (عادی و VIP) در یک صفحه، مستقیم از تنظیمات زنده‌ی پلتفرم — همیشه به‌روز، بدون نیاز به ویرایش دستی مستندات. دکمه‌ی جدید «📊 تعرفه‌ها و شرایط» در منوی ربات همین مقایسه را با اعداد زنده نشان می‌دهد.

### Docs

- کارمزد پیش‌فرض VIP در مستندات از ۱۰٪ به **۹٪** به‌روز شد (تعرفه واقعاً کم شده). راهنمای «استفاده‌ی هم‌زمان از هر دو سیستم» هم درباره‌ی کلید امضای کال‌بک اصلاح شد.

---

## [2.1.8] — سیستم تیکت پشتیبانی + چند بهبود و اصلاح

### Added

- **🎫 تیکت پشتیبانی داخل ربات.** از «📢 اخبار و پشتیبانی → 🎫 ثبت تیکت» مشکلتان را بنویسید؛ یک تیکتِ شماره‌دار ثبت می‌شود و پاسخ پشتیبانی از همین ربات برایتان می‌آید. با «📋 تیکت‌های من» هم وضعیت هر تیکت را می‌بینید. ارجاع از دستیار هوش‌مصنوعی هم به همین تیکت‌ها وصل شد.
- **📬 اطلاع‌رسانی تلگرامی برای فروشنده‌های VIP.** نتیجه‌ی بررسیِ آدرس برداشت (تایید/رد) و نتیجه‌ی نهایی هر برداشت (رسیدنِ ارز یا برگشتِ مبلغ) حالا خودکار از ربات برایتان ارسال می‌شود — دیگر لازم نیست داشبورد را مدام چک کنید.

### Changed

- **🏷 شناسه‌ی فروشگاه خودکار شد.** فروشنده‌ی جدید موقع ثبت‌نام دیگر اسم وارد نمی‌کند؛ خودکار شناسه‌ای مثل `cubepay-1000` می‌گیرد و ثبت‌نام یک مرحله کوتاه‌تر شد.
- **پیام‌های «واریز با مبلغ نامنطبق» شفاف‌تر شدند.** هر پیام حالا کدِ اختصاصی، ساعت پیامک، ۴ رقم کارت (اگر در پیامک بود) و فهرست بقیه‌ی فاکتورهای در انتظارتان را دارد — وقتی چند فاکتور و چند واریز هم‌زمان دارید، هر پیام خودش می‌گوید مالِ کدام واریز است.

### Fixed

- **پیامک تکراری دیگر پیام تکراری نمی‌سازد.** اگر یک پیامک بانکی چند بار به سیستم برسد (تکرار اپ فورواردر، یا دو کانال هم‌زمان)، فقط بار اول پردازش می‌شود — یک واریز، یک پیام، یک تصمیم.
- **دو مسیر مالی VIP تماماً «یا همه، یا هیچ» شدند.** ثبتِ اعتبار پرداختِ مشتری و فعال‌سازیِ اشتراک حالا اتمی‌اند؛ حتی اگر سرور در بدترین لحظه قطع شود، ممکن نیست فاکتورِ پرداخت‌شده بدون اعتبار بماند یا اشتراکِ پرداخت‌شده بدون سرویس.

### Docs

- سوال جدید در FAQ برای رایج‌ترین خطای مهاجرت به VIP: اگر با توکن `vip_` خطای «توکن نامعتبر است.» می‌گیرید، کدتان endpoint قدیمی را صدا می‌زند — آدرس باید `POST /pay/create-order.php` باشد و مبلغ به تومان در `price_amount`.

---

## [2.1.7] — لینک دانلود ثابت برای اپ SMS Forwarder

### Changed

- **اپلیکیشن اندروید حالا از GitHub دانلود می‌شود و لینکش دیگر عوض نمی‌شود.** آدرس تازه:

  ```
  https://github.com/cubepy/cubepay-doc/releases/download/android-latest/CubePay.apk
  ```

  این لینک همیشه به آخرین نسخه اشاره می‌کند، پس می‌توانید بوکمارکش کنید یا در راهنمای داخلیِ خودتان بگذارید و دیگر سراغش نروید. دکمه‌ی دانلود داخل ربات هم به همین آدرس می‌رود.

### Fixed

- **نسخه‌ای که از مستندات دانلود می‌شد با نسخه‌ای که دکمه‌ی ربات می‌داد یکی نبود.** فایل روی دو مسیر جدا نگه‌داری می‌شد و یکی از آن‌ها عقب مانده بود. حالا هر دو از یک منبع می‌آیند، پس این اختلاف دیگر ممکن نیست.

> اگر اپ را از قبل نصب کرده‌اید کاری لازم نیست؛ فقط برای آپدیت‌های بعدی از لینک تازه استفاده کنید.

---

## [2.1.6] — ظرفیت پذیرش VIP + استفاده‌ی هم‌زمان از هر دو سیستم

### Added

- **[راهنمای استفاده از هر دو سیستم با هم](./integrations/using-both-systems-guide.md)** — اگر اشتراک VIP دارید، مجبور نیستید یکی را انتخاب کنید. هر دو توکن هم‌زمان معتبرند و می‌توانید برای **هر سفارش** تصمیم بگیرید کدام مسیر برود: عادی برای سفارش‌های معمولی (بدون کارمزد، پول مستقیم به کارت خودتان)، VIP وقتی گوشیِ فورواردر خاموش است یا درآمد ارزی می‌خواهید.
- بخش **👑 سوالات CubePay VIP** در [`docs/FAQ.md`](./docs/FAQ.md).

### Changed

- **ظرفیت پذیرش VIP محدود شد.** چون این سرویس نگه‌داریِ امانیِ وجه را شامل می‌شود، تعداد فروشنده‌ها کنترل می‌شود. وقتی ظرفیت پر باشد، درخواست فعال‌سازی رد می‌شود و **هیچ لینک پرداختی ساخته نمی‌شود** — دکمه‌ی خرید هم در پنل نمایش داده نمی‌شود تا بی‌خود تلاش نکنید.

### 📌 سه قانونی که باید بدانید

- **اگر از قبل فروشنده‌ی VIP هستید، تمدیدتان هیچ‌وقت مسدود نمی‌شود.** سقف فقط برای فروشنده‌ی تازه‌وارد است — کسی که مدت‌هاست مشتری بوده نباید جایش را به یک تازه‌وارد بدهد.
- **لینک پرداختی که می‌گیرید تا ۲ ساعت جا نگه می‌دارد.** بین گرفتن لینک و پرداخت فاصله هست؛ این کار جلوی حالتی را می‌گیرد که چند نفر هم‌زمان آخرین جای باقی‌مانده را بخرند.
- **اگر پرداختتان تأیید شود، سرویس در هر شرایطی فعال می‌شود.** هیچ‌وقت پول گرفته نمی‌شود بدون اینکه سرویس داده شود.

---

## [2.1.5] — محاسبه‌ی مطمئن‌ترِ نرخ ارز برای برداشت

### Changed

- **نرخ تبدیل تومان به ارز حالا از چند منبع مستقل گرفته می‌شود.** پیش از این اگر تنها منبعِ نرخ در دسترس نبود، سیستم بی‌سروصدا از یک عدد ثابت استفاده می‌کرد — و اگر آن عدد با بازار فاصله گرفته بود، برداشت‌ها با نرخ نادرست انجام می‌شد بدون اینکه کسی متوجه شود. حالا اگر یک منبع قطع باشد، منبع بعدی امتحان می‌شود.
- **نرخ‌های غیرمنطقی رد می‌شوند.** هر نرخ تازه با آخرین نرخ معتبر مقایسه می‌شود؛ اگر اختلاف از حد مجاز بیشتر باشد پذیرفته نمی‌شود. این جلوی بدترین حالت را می‌گیرد: بعضی منابع نرخ **رسمی** ارز را می‌دهند نه نرخ آزاد، که حدود ۳۵٪ پایین‌تر است و باعث می‌شد حدود ۵۰٪ ارز بیشتر از حق شما پرداخت شود.
- **جهش واقعی بازار هم تشخیص داده می‌شود.** اگر همه‌ی منابع نرخ جدیدی بدهند و دو منبع مستقل روی آن توافق داشته باشند، یعنی بازار واقعاً جابه‌جا شده و نرخ جدید پذیرفته می‌شود — تا سیستم روی نرخ قدیمی گیر نکند.

> نرخ همچنان در **لحظه‌ی ثبت درخواست برداشت قفل** می‌شود و نوسان بعدی بازار روی برداشت شما اثری ندارد.

---

## [2.1.4] — رفع یک ایراد در حسابداری برداشت

### Fixed

- **برداشتی که ناموفق بسته شده بود، در شرایط نادری می‌توانست دوباره «تسویه‌شده» هم ثبت شود.** وقتی ارسال ارز ناموفق می‌شود، مبلغ بلافاصله به موجودی قابل‌تسویه‌ی شما برمی‌گردد؛ ولی امکان داشت همان برداشت بعداً به‌اشتباه موفق هم علامت بخورد و دفتر مالی ناسازگار شود.

  حالا **هر برداشت دقیقاً یک سرنوشت نهایی دارد** — یا موفق یا برگشتی، هرگز هر دو. این تضمین در سطح دیتابیس اعمال می‌شود، نه فقط با منطق برنامه.

> این ایراد هیچ‌وقت روی حساب هیچ فروشنده‌ای رخ نداده بود و هنگام بازبینیِ دوره‌ای پیدا شد. نیازی به اقدام از طرف شما نیست.

---

## [2.1.3] — رفع ایراد پنل مدیریت

### Fixed

- دکمه‌ی تنظیمات در فهرست فروشنده‌های پنل مدیریت باز نمی‌شد.

### Security

- **اطلاعات حساس دیگر در پاسخ‌های پنل مدیریت و تاریخچه‌ی تغییرات ذخیره نمی‌شوند.** توکن‌های فروشنده‌ها در جایی نمایش داده یا نگه‌داری می‌شدند که هیچ نیازی به آن نبود. توکن VIP فقط در همان لحظه‌ای نشان داده می‌شود که صراحتاً درخواست شود.

> این موارد فقط پنل مدیریت را تحت تأثیر قرار می‌دهند (که از قبل هم فقط برای مالک ربات قابل‌دسترس بود) و هیچ تغییری در API فروشنده‌ها ایجاد نمی‌کنند.

---

## [2.1.2] — سقف ماهانه برای فروشنده‌های VIP

### Added

- **سقف ماهانه‌ی پیش‌فرض: ۱۰۰٬۰۰۰٬۰۰۰ تومان.** تا پیش از این سقف ماهانه فقط وقتی اعمال می‌شد که ادمین برای همان فروشنده جداگانه تنظیم کرده بود؛ حالا مثل سقف روزانه روی همه اعمال می‌شود.

### 📌 نکته

سقف‌های حساب شما همیشه در پنل، تب **📖 هزینه‌ها و سقف‌ها** دیده می‌شود. اگر ادمین برای حساب شما سقف اختصاصی تعیین نکرده باشد، پیش‌فرض عمومی اعمال می‌شود — و اگر بعداً پیش‌فرض عوض شود، حساب شما هم بلافاصله همان را دنبال می‌کند. برای کسب‌وکارهای بزرگ‌تر، سقف‌ها جداگانه قابل افزایش است.

---

## [2.1.1] — مهاجرت به VIP بدون تغییر کد

### Added

- **حالا فقط کافی است توکن را عوض کنید.** اگر با روتر یکپارچه (`POST /pay/create-order.php`) کار می‌کنید — یعنی ربات Foxima، افزونه‌ی ووکامرس، یا کد اختصاصی خودتان — برای رفتن به VIP **هیچ تغییری در کدتان لازم نیست**. توکن `vip_` را جای توکن قبلی بگذارید، تمام. همان آدرس، همان فیلدها، همان پاسخ.
- برای اینکه کد فعلی‌تان دقیقاً مثل قبل کار کند، مسیر VIP همان نام فیلد مبلغ و همان کلید `method` را در پاسخ برمی‌گرداند.

### 📌 برای فروشنده‌های عادی

هیچ تغییری نیست. توکن عادی هیچ‌وقت با `vip_` شروع نمی‌شود، پس مسیر فعلی مو‌به‌مو دست‌نخورده می‌ماند.

---

## [2.1.0] — 👑 CubePay VIP: تسویه توسط CubePay (بدون نیاز به SMS Forwarder)

یک قابلیت **کاملاً اختیاری و موازی** کنار سیستم فعلی — نه جایگزین چیزی. مخصوص فروشنده‌هایی که نمی‌توانند SMS Forwarder نصب کنند: پول روی کارت خزانه‌ی CubePay جمع می‌شود، کارمزد کم می‌شود، و فروشنده هر وقت خواست به‌صورت ارزی برداشت می‌کند. فروشنده‌ای که این را فعال نکند، هیچ تفاوتی نمی‌بیند.

### Added

- **اشتراک ماهانه‌ی VIP** — فعال‌سازی با یک لینک پرداخت ارزی. بعد از پرداخت، حساب خودکار فعال می‌شود؛ ۳ روز قبل از انقضا هشدار تمدید می‌رود.
- **توکن اختصاصی VIP** (و نسخه‌ی آزمایشی‌اش) — جدا از توکن API عادی. با انقضای اشتراک فقط همین توکن برای **ساخت فاکتور** غیرفعال می‌شود؛ داشبورد، موجودی و **برداشت** همچنان کار می‌کنند.
- **موجودی در چهار مرحله** — «در انتظار» ← «قابل‌تسویه» ← «در حال تسویه» ← «تسویه‌شده». هر ریال قابل ردیابی است و هیچ عددی دستی ویرایش نمی‌شود.
- **برداشت ارزی خودکار** — با قفل‌شدن نرخ در لحظه‌ی ثبت درخواست، فقط به آدرسی که ادمین تأیید کرده، و برگشت **دقیقاً یک‌بار** مبلغ در صورت شکست.
- **سقف‌ها روی سرور اعمال می‌شوند** — هر فاکتور، روزانه و ماهانه؛ نه فقط در پنل.
- **تشخیص فاکتور تکراری** — بر اساس ترکیب فروشنده + مبلغ + مشتری + بازه‌ی زمانی، نه فقط مبلغ. پس دو سفارش واقعی هم‌مبلغ به‌اشتباه مسدود نمی‌شوند.
- **پنل اختصاصی** — تب VIP در پنل فروشنده: داشبورد، فاکتورها، کیف‌پول، برداشت، و تب **📖 هزینه‌ها و سقف‌ها** که همه‌ی اعداد حیاتی را یک‌جا نشان می‌دهد.
- **مستندات** — [`docs/CUBEPAY-VIP-API-REFERENCE.md`](./docs/CUBEPAY-VIP-API-REFERENCE.md).

### Changed

- **ساخت دستی فاکتور در این سیستم ممکن نیست** — نه از پنل فروشنده، نه از ربات، نه حتی توسط ادمین. تنها راه، فراخوانی API از سایت یا ربات خودتان است.
- **`order_id` برای همیشه یکتاست** — برخلاف مسیر کارتی فعلی، حتی بعد از لغو فاکتور هم دوباره قابل‌استفاده نیست.
- مبلغ‌ها در این سیستم همه به **تومان**اند (نه ریال).

### 🔒 جدایی کامل از مسیرهای فعلی

مسیر کارت‌به‌کارت SMS Forwarder و درگاه ارزی ۳٪ فروشنده‌های عادی **هیچ تغییری نکرده‌اند**. حساب‌ها، کارمزدها و توکن‌ها کاملاً جدا هستند و کیف‌پول کارمزد اصلاً در مسیر VIP بررسی نمی‌شود.

---

## [2.0.1] — رفع باگ: عدم ارسال callback پرداخت‌های کریپتویی

### Fixed

- **callback کریپتو اصلاً فرستاده نمی‌شد** — به‌خاطر یه خط جاافتاده تو وبهوک IPN، وقتی یه پرداخت کریپتویی به وضعیت نهایی (`finished`/`failed`/`expired`) می‌رسید، اطلاع‌رسانی به `callback_url` فروشنده هیچ‌وقت واقعاً اتفاق نمی‌افتاد — با این‌که تابعش کامل نوشته و امضا (HMAC) هم توش پیاده‌سازی شده بود. این یعنی فروشنده‌هایی که فقط به callback تکیه کرده بودن (نه به `check-order-status.php`)، از تکمیل پرداخت کریپتویی مشتری‌هاشون خودکار مطلع نمی‌شدن. الان درست شده.
- **بدون تلاش مجدد و بدون تایید تحویل** — ارسال callback قبلاً فقط یک‌بار انجام می‌شد و نتیجه‌ش (موفق/ناموفق بودنِ واقعیِ رسیدنش به سرور فروشنده) هیچ‌وقت چک نمی‌شد. حالا تا ۳ بار با فاصله تلاش می‌کنه و کد HTTP پاسخ فروشنده رو هم بررسی و لاگ می‌کنه.

### 📌 توصیه برای فروشنده‌ها

هرچند این باگ برطرف شده، پیشنهاد می‌کنیم علاوه بر تکیه به callback، یه بررسی دوره‌ای هم با [`check-order-status.php`](./docs/CRYPTO-API-REFERENCE.md#-استعلام-وضعیت-سفارش-check-order-status) برای سفارش‌های هنوز «در انتظار» داشته باشید — شبکه همیشه غیرقابل پیش‌بینیه، این یه لایه‌ی اطمینان اضافیه.

---

## [2.0.0] — تبدیل CubePay به Payment OS (Long-Polling، Risk Engine، Sandbox، Developer Portal، Settlement، Queue)

این نسخه بزرگ‌ترین آپدیت تا این‌جای CubePayه — عملاً گیت‌وی رو از یه «تاییدکننده‌ی کارت‌به‌کارت» به یه زیرساخت مالی کامل‌تر تبدیل می‌کنه. هیچ تغییری تو قرارداد API فعلی (`create-payment`, `verify-payment`) ایجاد نشده؛ همه‌چیز اضافه‌ست، نه جایگزین.

### Added

- **Long-Polling برای وضعیت پرداخت** —  به‌جای جواب فوری، تا ۲۰ ثانیه باز می‌مونه و همین که وضعیت عوض بشه بلافاصله جواب می‌ده. تاخیرِ تاییدِ لحظه‌ای از ~۴ ثانیه (polling قدیمی) به ~۱ ثانیه رسید، بدون نیاز به WebSocket/زیرساخت جدید. مقدار زمان از قابل‌تنظیمه.
- **Ledger واقعی کیف‌پول** — تب «کیف‌پول» حالا «موجودی قابل‌استفاده» و «در انتظار» (کارمزدهای رزروشده‌ی فاکتورهای بازِ هنوز‌منقضی‌نشده) رو جدا نشون می‌ده، به‌همراه تاریخچه‌ی کامل هر تراکنش کیف‌پول.
  
- **Risk Engine (فاز مشتری)** — هر فاکتور، موقع اولین بازدید صفحه‌ی پرداخت، یه امتیاز ریسک ۰-۱۰۰ می‌گیره (بر اساس IP تکراری، چند فروشنده‌ی متفاوت از یه IP، انحراف مبلغ از میانگین فروشنده، User-Agent مشکوک). فقط یه سیگنال کمکیه، هیچ‌جا پرداخت رو مسدود نمی‌کنه.
  
- **Risk Engine (فاز فروشنده)** — منوی ادمین `🚨 ریسک فروشنده‌ها`: فروشنده‌هایی که نرخ مغایرت واریزی بالا، نرخ شکست بالا، میانگین ریسک مشتری بالا، یا جهش ناگهانی حجم دارن رو فهرست می‌کنه.
  
- **Merchant Dashboard (تب آنالیتیکس)** — نمودار گردش ۱۴ روزه، نرخ موفقیت، میانگین مبلغ تراکنش، ساعت‌های پرتراکنش، نسبت تایید خودکار به دستی.
  
- **Sandbox Mode** — هر فروشنده یه توکن آزمایشی (`test_...`) جدا از توکن واقعی داره؛ فاکتورهای ساخته‌شده باهاش نه کارمزد واقعی کم می‌کنن نه به کارت واقعی نیاز دارن، و تو صفحه‌ی پرداخت دکمه‌ی «شبیه‌سازی موفق/ناموفق» جای شماره کارت میاد. برای تست اتصال قبل از رفتن به حالت واقعی.

- **Developer Portal** — مستندات کامل + API Explorer زنده در ؛ می‌شه مستقیم با توکن آزمایشی یه فاکتور ساخت و شبیه‌سازی کرد، بدون نوشتن کد.
  
- **API Versioning** — همه‌ی endpointهای اصلی حالا زیر `/api/v1/` هم در دسترسن (آدرس قدیمی هم بدون تغییر کار می‌کنه)، برای این‌که آپدیت‌های آینده هیچ‌وقت فروشنده‌های فعلی رو نشکنه.
  
- **Settlement Engine برای کیف‌پول ارز دیجیتال** — درخواست برداشتِ درخواستی (نه فقط زمان‌بندی‌شده)؛ فروشنده‌های VIP بلافاصله تسویه‌ی واقعی می‌گیرن، بقیه با تایید ادمین.
  
- **بهینه‌سازی صفِ پردازش پیامک** — وب‌هوک پیامک حالا فوراً بعد از ثبت پرداخت به اپ فورواردر جواب می‌ده و کارهای کندِ شبکه‌ای (تلگرام، وب‌هوک فروشنده) رو بعدش انجام می‌ده — برای جلوگیری از تایم‌اوت و ریترایِ بی‌مورد رو حجم بالا.

### Fixed

- رفع باگ Multi-payment Router: تو مسیر «هر دو روش پرداخت»، فاکتورهای کارتی هیچ‌وقت ذخیره نمی‌شد؛ در نتیجه برای پرداخت‌های کارتی همیشه «pending» برمی‌گردوند حتی بعد از پرداخت موفق. الان درست ذخیره و خونده می‌شه.
- لینک «📚 مستندات API» (هم تو ربات، هم تو Mini App) که به یه ریپوی خالی/غیرفعال گیت‌هاب اشاره می‌کرد، به Developer Portal واقعی وصل شد.

### Security

- امتیاز ریسک و سیگنال‌های آنومالی، صرفاً کمکیِ بررسیِ دستی‌ان و هیچ تصمیم خودکاری (مسدودسازی، رد پرداخت) نمی‌گیرن.
-  (مخصوص Sandbox) قبل از هر کاری چک می‌کنه فاکتور واقعاً آزمایشی باشه؛ روی فاکتور واقعی همیشه ۴۰۳ می‌گیره.



---

## [1.13.0] — امنیت، چند-ولتی، و کال‌بک کریپتو به سایت فروشنده

### Added

- **کال‌بک پرداخت کریپتویی به سایت فروشنده** — تا الان فقط پرداخت کارتی به `callback_url` فروشنده اطلاع می‌داد؛ حالا پرداخت کریپتویی هم (با پارامتر جدید `callback_url` در `create-crypto-payment.php` و روتر یکپارچه) همین کارو می‌کنه، با امضای HMAC-SHA256 (کلید = توکن API فروشنده) برای جلوگیری از جعل.
- بعد از پرداخت کریپتویی موفق/ناموفق، مرورگر مشتری خودکار به سایت فروشنده برمی‌گرده (دقیقاً مثل رفتار فعلیِ پرداخت کارتی).
- **افزونه‌ی ووکامرس نسخه‌ی ۱.۱.۰** — پشتیبانی از پرداخت ارز دیجیتال از طریق یه فیلد تنظیماتِ جدیدِ اختیاری (سازگار کامل با نسخه‌ی قبل)؛ مستندات فنی جدید: [`docs/CRYPTO-API-REFERENCE.md`](./docs/CRYPTO-API-REFERENCE.md)
- **چند ولت کریپتو برای هر فروشنده** — قبلاً هر فروشنده فقط یه آدرس/ارز می‌تونست ثبت کنه؛ اگه مشتری‌هاش با چند ارز پرداخت می‌کردن، تسویه‌ی ارزهای دیگه گیر می‌کرد. حالا برای هر ارز (USDT-BEP20 / TRX / TON) جدا آدرس ثبت می‌شه.
- پنل ادمین «ولت‌های کریپتوی فروشنده‌ها» حالا به تفکیک هر (فروشنده، ارز) لیست می‌کنه، نه فقط هر فروشنده.

### Fixed

- 🔴 **باگ امنیتیِ مهم:** اسکریپت‌های مالیِ کران‌جاب (تسویه‌ی هفتگی/روزانه، برداشت کارمزد، انقضای فاکتورهای رهاشده) هیچ محافظتی نداشتن و از طریق مرورگر هم قابل‌اجرا بودن. حالا فقط از خط‌فرمان (CLI) اجرا می‌شن.
- 🔴 لاگ‌ها، فایل‌های SQLite، و فایل‌های کانفیگ تو پوشه‌های وب‌قابل‌دسترس بودن و لیستِ محتوای پوشه هم فعال بود؛ بسته شد.
- یه باگ نمایشی تو صفحه‌ی پرداخت کریپتویی که باعث می‌شد بعد از انتخاب ارز، آدرس واریز نمایش داده نشه (خودِ پرداخت درست ثبت می‌شد، فقط نمایشش خراب بود).

### Security

- امضای HMAC-SHA256 برای کال‌بک پرداخت کریپتویی (جزئیات کامل در [`docs/CRYPTO-API-REFERENCE.md`](./docs/CRYPTO-API-REFERENCE.md))
- اسکریپت‌های کران‌جاب حساس، محدود به اجرای CLI شدن
- دسترسی مستقیم به لاگ‌ها/کانفیگ‌ها/دیتابیس‌های محلی از طریق وب مسدود شد

---

## [1.12.0] — پرداخت ارز دیجیتال + یکپارچه‌سازی کارت و کریپتو

### Added

- **پرداخت با ارز دیجیتال (کریپتو)** — علاوه بر کارت‌به‌کارت، حالا مشتری‌ها می‌تونن با **USDT (شبکه‌ی BEP20/BSC)، TRX، یا TON** هم پرداخت کنن. تسویه از طریق NOWPayments و مکانیزم Sub-partner (هر فروشنده یه حساب مجزا و مستقل داره) انجام می‌شه.
- **مشتری خودش ارز رو انتخاب می‌کنه** — دقیقاً مثل درگاه‌های کریپتوی معروف؛ فروشنده لازم نیست از قبل ارز رو مشخص کنه.
- **روتر یکپارچه‌ی پرداخت (`pay/create-order.php`)** — یک endpoint واحد که، بسته به تنظیمات فروشنده، مستقیم فاکتور کارتی می‌سازه، مستقیم فاکتور کریپتویی می‌سازه، یا یه صفحه‌ی «کارت یا کریپتو؟» به مشتری نشون می‌ده و بر اساس انتخابش هدایت می‌کنه.
- **فعال/غیرفعال‌کردن مستقل هر روش پرداخت** — از منوی جدید «💳 روش‌های پرداخت»، فروشنده می‌تونه کارت‌به‌کارت و کریپتو رو جدا از هم روشن/خاموش کنه (با این محدودیت که حداقل یکی همیشه فعال بمونه).
- **انتخاب بازه‌ی برداشت کریپتو توسط خودِ فروشنده** — هفتگی، روزانه، یا لحظه‌ای (بلافاصله بعد از هر پرداختِ موفق).
- **مدیریت ولت کریپتو** — فروشنده از «💰 ولت پرداخت کریپتو» آدرس و ارز دریافتی خودش رو ثبت می‌کنه، موجودیِ زنده‌ی تسویه‌نشده رو می‌بینه، و می‌تونه با دکمه‌ی «💸 برداشت الان» زودتر از موعدِ بازه‌ی تنظیم‌شده هم برداشت کنه.
- **پنل ادمین برای مدیریت whitelist ولت‌ها** — لیست کامل آدرس‌های ثبت‌شده‌ی فروشنده‌ها، جدا برای «در انتظار تایید» و «تایید‌شده».
- **مهلت اعتبار ۶۰ دقیقه‌ای برای فاکتورهای کریپتویی** (مشابه فاکتورهای کارتی) + شمارش‌معکوس زنده تو صفحه‌ی پرداخت.
- **تست کامل بدون کدنویسی** — «🧾 ساخت فاکتور دستی» حالا سه گزینه داره: کارت، کریپتو، یا هر دو (برای دیدن دقیق تجربه‌ی مشتری واقعی، با هر ترکیب تنظیماتی).
- بازطراحی صفحات پرداخت کریپتویی و صفحه‌ی «انتخاب روش» با همون زبان طراحیِ صفحه‌ی کارت‌به‌کارت (فونت Vazirmatn، طرح تیکت، رنگ برند) تا تجربه‌ی کاربری در کل مسیر یکدست باشه.

### Changed

- کارمزد پلتفرم از تراکنش‌های کریپتویی به‌صورت درصدی (نه مبلغ ثابت تومانی) محاسبه می‌شه، چون مبلغ به ارز دیجیتاله نه ریال.
- کیف‌پول تومانیِ کارمزد (مربوط به کارت‌به‌کارت) و بالانس کریپتویی هر فروشنده **کاملاً مستقل و بدون هیچ اشتراکی** نگه‌داری می‌شن، حتی اگه فروشنده هر دو روش رو هم‌زمان فعال کنه.

### Fixed

- نمایش تاریخ/ساعت تراکنش‌ها تو ربات، که قبلاً میلادی و با زون سرور نمایش داده می‌شد، به تقویم شمسی و ساعت واقعی تبدیل شد.

### Security

- احراز هویت IPN دریافتی از NOWPayments با امضای HMAC-SHA512 چک می‌شه.
- ساخت پرداخت کریپتویی از سمت مشتری (تو مرورگر) هیچ‌وقت به توکن API فروشنده دسترسی نداره؛ همه‌ی درخواست‌های حساس سمت سرور انجام می‌شن.

---

## [1.11.0] — پنل وب فروشنده‌ی تعاملی + تست اتصال پیامک

### Added

- **پنل وب فروشنده تکمیل شد** — علاوه بر چیزهای قبلی (کیف‌پول، تراکنش‌ها، مغایرت‌ها)، حالا از تو خود Mini App هم می‌شه: کارت‌ها رو کامل مدیریت کرد (افزودن/حذف/فعال‌سازی/حالت چرخش)، درصد جبران کارمزد رو عوض کرد، فاکتور دستی ساخت، روش دریافت پیامک بانکی (وب‌هوک/ملی‌پیامک) رو انتخاب و تست کرد، اطلاعات زیرمجموعه‌گیری رو دید، و خروجی اکسل گرفت.
- **دکمه‌ی «🧪 تست اتصال پیامک»** — یه فاکتور تستی رایگان می‌سازه و یه پیامک بانکی مصنوعی از مسیر واقعی (وب‌هوک یا ملی‌پیامک) می‌فرسته تا بدون واریز واقعی مطمئن بشید پایپ‌لاینتون سالمه.
- **شماره‌ی فوروارد جدا برای روش ملی‌پیامک** — برای وقتی حساب تلگرامتون با گوشی/سیمکارت بانکتون فرق داره.
- **هشدار قوانین قبل از شارژ اولیه** — قبل از پرداخت، صراحتاً تایید می‌کنید که قوانین و مستندات رو خوندید و مبلغ غیرقابل‌بازگشته.

### Changed

- منوی ربات به‌طور کامل مرتب و دسته‌بندی شد (به‌جای فهرست بلند دکمه‌ها، چند دکمه‌ی گروه‌بندی‌شده با زیرمنو) و دکمه‌ها دوتا-دوتا کنار هم چیده شدن.
- دکمه‌ی «آموزش اتصال پیامک» به «انتخاب روش تاییدیه مبلغ واریزی» تغییر نام داد.
- راهنمای اتصال اپ فورواردر برای روش ملی‌پیامک هم اضافه شد (قبلاً فقط برای وب‌هوک بود).

### Fixed

- 🔴 فرمت شماره‌ای که ملی‌پیامک برای فرستنده‌ی پیامک می‌فرسته (بدون صفر ابتدایی) قبلاً نامعتبر تشخیص داده می‌شد.
- یه مشکل موقت که باعث می‌شد پنل وب فروشنده در بعضی موارد رو «در حال بارگذاری» بمونه، رفع شد.

---

## [1.10.0] — روش دوم تاییدیه پیامک (ملی‌پیامک) + مرتب‌سازی منوها

### Added

- **روش دوم تشخیص واریز: فوروارد پیامک از طریق ملی‌پیامک** — علاوه بر وب‌هوک (URL)، فروشنده حالا می‌تونه پیامک بانکش رو مستقیم (SMS به SMS) به یه سرشماره‌ی ملی‌پیامک فوروارد کنه؛ تشخیص فروشنده از روی شماره موبایل فرستنده انجام می‌شه، بدون نیاز به اینترنت دائمی گوشی
- فروشنده می‌تونه یه **شماره‌ی فوروارد جدا از شماره‌ی ثبت‌نام حسابش** تعریف کنه (برای وقتی که حساب تلگرام با یه گوشی/سیمکارته ولی پیامک بانکی از یه شماره‌ی دیگه میاد)
- ادمین از یه صفحه‌ی تنظیمات جدید («📡 روش دریافت پیامک بانکی») می‌تونه:
  - وب‌هوک و ملی‌پیامک رو مستقل از هم روشن/خاموش کنه (خاموش‌کردن هرکدوم، توکن‌های همون روش رو غیرفعال می‌کنه)
  - سرشماره‌ی ملی‌پیامک و توکن سراسری‌ش رو تنظیم/بازتولید کنه
- راهنمای گام‌به‌گام اپ فورواردر برای روش ملی‌پیامک (مقصد = Phone Number/SMS به‌جای URL)، هم‌سطح راهنمای وب‌هوک
- **امکان ثبت شماره شبا برای هر فروشنده** توسط ادمین (از داخل «مدیریت فروشنده‌ها»)
- **شماره شبای پلتفرم** برای شارژ کیف‌پول فروشنده‌ها، جدا و کنار کارت قبلی
- **هشدار قوانین قبل از شارژ اولیه**: پیام شارژ اولیه حالا صراحتاً اعلام می‌کنه که پرداخت یعنی قبول قوانین، مطالعه‌ی کامل مستندات گیت‌هاب، و غیرقابل‌بازگشت بودن مبلغ؛ فروشنده باید قبل از دیدن دکمه‌ی پرداخت واقعی، این هشدار رو صراحتاً تایید کنه
- منوی ادمین به دو سطح تقسیم شد: منوی اصلی فقط «🛠 مدیریت» و «🏪 فروشگاه خودم»؛ همه‌ی بخش‌های دیگه (فروشنده‌ها، کارمزد و آمار، تنظیمات سیستم، اطلاع‌رسانی و لاگ) زیرمجموعه‌ی «مدیریت» شدن

### Changed

- منوهای فروشنده و ادمین که شلوغ شده بودن، به دسته‌های کوچیک‌تر و دکمه‌های دوتا-دوتا (به‌جای زیر هم) بازطراحی شدن
- دکمه‌ی «آموزش اتصال پیامک» به «انتخاب روش تاییدیه مبلغ واریزی» تغییر نام داد تا واضح‌تر باشه

### Fixed

- 🔴 **باگ تشخیص شماره موبایل:** فرمت شماره‌ای که ملی‌پیامک برای فرستنده‌ی پیامک می‌فرسته (بدون صفر ابتدایی، مثل `9123456789`) قبلاً به اشتباه نامعتبر تشخیص داده می‌شد و باعث می‌شد تراکنش‌های واقعی تایید نشن؛ این فرمت هم پشتیبانی شد

---

## [1.9.1] — رفع مشکلات UX پرداخت + امنیت

### Fixed

پیام گمراه‌کننده‌ی «قبلاً شارژ شده» در صفحه‌ی نتیجه‌ی شارژ کیف‌پول — حالا در این حالت هم پیام موفقیت با موجودی واقعی نشون داده می‌شه

مبلغ صفحه‌ی پرداخت از تومان به ریال تغییر کرد تا با اپ بانک‌ها هم‌خوانی داشته باشه

دکمه‌ی «بازگشت به ربات» به صفحات پرداخت/شارژ کیف‌پول اضافه شد

حذف دکمه‌ی ریپلای «داشبورد وب» از منوها — جایگزین شده با Menu Button تلگرام (initData پایدارتر)

### Added

تشخیص واریز با مبلغ نامنطبق (کم/زیاد از مبلغ فاکتور) + اعلان به فروشنده با دکمه‌ی تایید/رد دستی

---

## [1.9.0] — امنیت و مستندات

### Added

- **شارژ اولیه‌ی اجباری** — فروشنده‌ی جدید قبل از فعال شدن توکن/وب‌هوک، باید یه مبلغ تست (پیش‌فرض ۴۰,۰۰۰ تومان) واریز کنه؛ هم از تو ربات هم از تو خود API کنترل می‌شه
- **محدودیت نرخ درخواست API** — علاوه بر وب‌هوک پیامک، حالا `create-payment` و `verify-payment` هم محدودیت درخواست دارن (۶۰ در دقیقه به ازای هر فروشنده)
- بازسازی کامل مستندات: `START-HERE.md` (راهنمای انتخاب مسیر)، `generic-integration-guide.md` (راهنمای اتصال بدون Foxima)، `API-REFERENCE.md` (مرجع فنی جدا از README)

### Changed

- زمان انقضای فاکتور از ۳۰ به ۱۵ دقیقه قابل‌تنظیم (پیش‌فرض پلتفرم)
- امکان تعیین زمان انقضای اختصاصی برای هر فاکتور دستی (۵ تا ۱۴۴۰ دقیقه)

---

## [1.8.0] — رفع باگ مهم کارمزد + زیرمجموعه‌گیری

### Fixed

- 🔴 **باگ مهم منطق کارمزد:** قبلاً درصد تقسیم، مبلغ کسرشده از کیف‌پول رو هم کم می‌کرد. حالا کارمزد ثابت **همیشه کامل** از کیف‌پول فروشنده کسر می‌شه؛ درصد فقط تعیین می‌کنه چقدرش از مشتری جبران بشه
- 🔴 **باگ مهم امنیتی:** اگه فروشنده‌ای کارت ثبت نکرده بود، فاکتورش به‌جای خطا، از کارت مالک پلتفرم استفاده می‌کرد (ریسک واریز پول به شخص اشتباه). حالا بدون کارت، هیچ فاکتوری (نه خودکار، نه دستی) ساخته نمی‌شه

### Added

- **سیستم زیرمجموعه‌گیری** — کد/لینک دعوت اختصاصی هر فروشنده؛ درصدی (پیش‌فرض ۱۵٪، قابل‌تغییر) از کارمزد واقعی زیرمجموعه به کیف‌پول دعوت‌کننده اضافه می‌شه — از سود پلتفرم کسر می‌شه، نه از زیرمجموعه، پس ذاتاً امن در برابر سوءاستفاده
- ادمین می‌تونه حساب فروشندگی خودش (توکن، کارت‌ها) رو هم از تو ربات ببینه
- کارت اختصاصی و جدا برای «شارژ کیف‌پول فروشنده‌ها» — کاملاً مجزا از کارت‌های فروش خود پلتفرم

---

## [1.7.0] — تجربه‌ی کاربری و برندینگ

### Added

- پیام‌های فاکتور (شارژ کیف‌پول / فاکتور دستی) بعد از پرداخت **خودکار ادیت** می‌شن — دکمه‌ی بلااستفاده دیگه نمی‌مونه
- بازطراحی و رفع اسم «زرین‌پی» در تمام لایه‌های Faoxima (چت ادمین، پنل وب، لیست وضعیت درگاه‌ها) — «کیوب‌پی» جایگزین شد
- بسته‌ی «فایل‌های آماده» برای اتصال سریع Faoxima، بدون ویرایش دستی

---

## [1.6.0] — بونوس و ایمنی کیف‌پول

### Added

- بونوس پلکانی شارژ کیف‌پول (۵٪ تا ۲۰٪ بسته به مبلغ)
- بررسی کامل و مستند مسیر مالی: قفل اتمیک، مسدودسازی تراکنش بدون موجودی کافی، اعلان مستقیم فروشنده وقتی موجودی کم می‌شه

---

## [1.5.0] — چندکارتی و امنیت بیشتر

### Added

- نمایش اختیاری شبا در کنار کارت (فقط برای انتقال آنی)
- افزایش طول شناسه‌ی یکتای فاکتور (authority) از ۳۲ به ۵۰ کاراکتر
- **فاکتور دستی** — ساخت لینک پرداخت از تو ربات، بدون کد نویسی
- نمایش لحظه‌ای مبلغ کارمزد فروشنده تو «پنل من»

---

## [1.4.0] — شفافیت و گزارش‌گیری

### Added

- تاییدیه‌ی قوانین و شرایط موقع ثبت‌نام فروشنده
- خروجی اکسل (CSV) کامل تراکنش‌ها از تو ربات
- مستندات FAQ و Changelog رسمی

---

## [1.3.0] — احراز هویت و پایداری

### Added

- عضویت اجباری در کانال تلگرام برای استفاده از ربات
- احراز هویت شماره تلفن با اشتراک‌گذاری کانتکت واقعی تلگرام (نه تایپ دستی)
- دکمه‌ی «منوی اصلی» در همه‌ی بخش‌ها
- راهنمای دانلود و پیکربندی اپ فورواردر پیامک، با هشدار برجسته درباره‌ی نیاز به اینترنت دائم گوشی

### Fixed

- اسکیپ‌نشدن اسم کسب‌وکار/کارت در پیام‌های HTML تلگرام که می‌تونست پیام رو کلاً خراب کنه
- بخش «مدیریت فروشنده‌ها» یه ستون قدیمی و بلااستفاده رو برای کارت نشون/ویرایش می‌کرد

---

## [1.2.0] — چندفروشنده‌ای کامل

### Added

- چند کارت برای هر فروشنده + حالت چرخش (دستی / رندوم / ساعتی)
- کارمزد اختصاصی قابل‌تنظیم برای هر فروشنده
- شارژ خودکار کیف‌پول از داخل ربات
- اعلان تلگرامی مستقیم به فروشنده بعد از هر تراکنش موفق
- آمار کلی تراکنش‌ها برای ادمین (روزانه/هفتگی/ماهانه/کل)
- مدیریت کامل فروشنده‌ها از ربات (ویرایش، تغییر وضعیت، حذف)

### Fixed

- رزرو کارمزد به‌صورت اتمیک شد تا کیف‌پول در شرایط همزمانی بالا منفی نره
- جلوگیری از ثبت‌نام تکراری با قفل یکتا روی آیدی تلگرام

### Security

- جلوگیری از SSRF روی آدرس کال‌بک فروشنده‌ها
- محدودیت نرخ درخواست روی وب‌هوک پیامک

---

## [1.1.0] — تبدیل به پلتفرم

### Added

- تبدیل از سیستم تک‌کاربره به پلتفرم چند-فروشنده‌ای
- ربات مدیریت فروشندگان جدا (ثبت‌نام، تایید ادمین، پنل فروشنده)
- SDK رسمی PHP (`CubePayClient.php`)

---

## [1.0.0] — نسخه‌ی اولیه

### Added

- ساخت و تایید تراکنش کارت‌به‌کارت با تشخیص خودکار از روی پیامک بانکی
